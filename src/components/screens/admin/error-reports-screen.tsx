import ErrorReportsScreenClient from "@/components/screens/admin/error-reports-screen-client";

/** 서버 래퍼: 클라이언트 의존 로직을 하위 컴포넌트로 분리한다. */
export default function ErrorReportsScreen() {
  return <ErrorReportsScreenClient />;
}

